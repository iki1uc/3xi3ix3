export const PREFETCH = {

  // --------------------------------------------------
  // 1) POSITION — ULTRA-U / Layer / Relation / Direction
  // --------------------------------------------------
  POS: {
    mode: "ULTRA-U",
    layer: [3, 6, 9],
    relation: ["3x3", "6E6D", "9x9"],
    direction: {
      in: "horizontal",
      out: "vertikal"
    }
  },

  // --------------------------------------------------
  // 2) ENGINE — ULTRA-KERNEL / Bindungen
  // --------------------------------------------------
  ENGINE: {
    mode: "ULTRA-KERNEL",
    ctrl: true,
    bind: ["9x9", "3x3", "6E6D"]
  },

  // --------------------------------------------------
  // 3) 9x9 — Matrix-Bindung
  // --------------------------------------------------
  MATRIX9: {
    in: "horizontal",
    out: "vertikal",
    breite: 9,
    tiefe: 9,
    myster: true,
    anchor: 1,
    validate: true
  },

  // --------------------------------------------------
  // 4) 3x3 — Axiom / Zoom / Score
  // --------------------------------------------------
  MATRIX3: {
    axiom: ["DA", "NE", "BEN"],
    zoom: [1, 3, 9],
    best: {
      score: "100%",
      mode: "ULTRA-SCORE",
      core: ["S", "M", "U"],
      output: "MAX"
    }
  },

  // --------------------------------------------------
  // 5) 6E6D — Ableitung / Tiefen-Kernel / Zoom
  // --------------------------------------------------
  ENGINE6: {
    ableitung: "U",
    kernel: "Tiefen-Kernel",
    zoom: [6, 12, 18],
    best: {
      boost: true
    }
  },

  // --------------------------------------------------
  // 6) KI — Frame / State / Switch
  // --------------------------------------------------
  KI: {
    mode: "ENGINE-FRAME",
    recall: false,
    history: false,
    traceback: false,
    state: "CURRENT",
    switch: "INSTANT"
  },

  // --------------------------------------------------
  // 7) PREFETCH — ULTRA / Zoom / Trace
  // --------------------------------------------------
  CORE: {
    mode: "ULTRA",
    engine: true,
    zoom: [1, 3, 6, 9],
    trace: false
  },

  // --------------------------------------------------
  // 8) SYNC — global
  // --------------------------------------------------
  SYNC: {
    all: true,
    ultra: true,
    engine: true
  }
};
