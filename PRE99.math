export const PREFETCH_POS_ENGINE_MATRIX_SYNC = {

  // --------------------------------------------------
  // 1) POS — ULTRA-U / Layer / Relation
  // --------------------------------------------------
  POS: {
    MODE: "ULTRA-U",
    LAYER: [3, 6, 9],
    RELATION: ["3x3", "6E6D", "9x9"],
    axis: "ORG-REORG"
  },

  // --------------------------------------------------
  // 2) ENGINE — ULTRA-KERNEL
  // --------------------------------------------------
  ENGINE: {
    MODE: "ULTRA-KERNEL",
    CTRL: true,
    BIND: ["9x9", "3x3", "6E6D"],
    axis: "NC-ORG"
  },

  // --------------------------------------------------
  // 3) MATRIX — 9x9 / 3x3 / 6E6D
  // --------------------------------------------------
  MATRIX: {

    // 9x9 — ORG
    "9x9": {
      IN: "horizontal",
      OUT: "vertikal",
      BREITE: 9,
      TIEFE: 9,
      MYSTER: "aktiv",
      axis: "ORG"
    },

    // 3x3 — REORG
    "3x3": {
      AXIOM: ["DA", "NE", "BEN"],
      ZOOM: [1, 3, 9],
      axis: "REORG"
    },

    // 6E6D — NC-ORG
    "6E6D": {
      ABLEITUNG: "U",
      KERNEL: "Tiefen-Kernel",
      ZOOM: [6, 12, 18],
      axis: "NC-ORG"
    }
  },

  // --------------------------------------------------
  // 4) SYNC — global
  // --------------------------------------------------
  SYNC: {
    ALL: true,
    ULTRA: true,
    ENGINE: true,
    axis: "ORG-REORG-NC"
  }
};
